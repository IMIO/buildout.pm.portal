[buildout]

allow-picked-versions = false
show-picked-versions = true

extends =
    versions.cfg

versions = versions

parts +=
    instance

eggs +=
    plonemeeting.portal.core

zcml +=
    plonemeeting.portal.core

[instance]
eggs +=
    ${buildout:eggs}

zcml +=
    ${buildout:zcml}
